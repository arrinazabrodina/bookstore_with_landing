@model BookStoreWebApplication.Models.Genre

@{
    ViewData["Title"] = $"Деталі {Model.Name}";
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Деталі</h1>

        <div>
            <h4>Жанр @Html.DisplayFor(model => model.Name)</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning">Редагувати</a> |
            <a asp-action="Index" class="btn btn-primary">Повернутись до жанрів</a>
        </div>
    </div>
</body>
</html>
